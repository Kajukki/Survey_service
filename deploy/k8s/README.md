# Kubernetes

Add Kustomize bases under `base/` and environment overlays under `overlays/dev` and `overlays/prod`.

Resources to define later: Deployments (api, web, worker), CronJob (scheduler), Service, Ingress (nginx), Secrets, ConfigMaps, HPA.

See [architecture](../../docs/architecture.md).
