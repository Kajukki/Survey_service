# Docker images

Multi-stage builds will copy artifacts from `apps/*` when implementations exist. Current Dockerfiles are minimal placeholders so CI can validate syntax.

Image names (suggested): `survey-service-web`, `survey-service-api`, `survey-service-worker`, `survey-service-scheduler`.
