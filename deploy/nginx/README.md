# NGINX snippets

Optional include files or reference configs if your cluster uses more than Ingress annotations alone.

Production routing is typically defined in Kubernetes **Ingress** (nginx ingress controller). See [architecture](../../docs/architecture.md) section 9.
